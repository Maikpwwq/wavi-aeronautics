'use client'
import Typography from '@mui/material/Typography'
import Box from '@mui/material/Box'

export default function AdminSettings() {
  return (
    <Box>
      <Typography variant="h4" sx={{ mb: 4, fontWeight: 'bold' }}>Configuración</Typography>
      <Typography variant="body1">Configuración del portal en desarrollo.</Typography>
    </Box>
  )
}
