'use client'
import Typography from '@mui/material/Typography'
import Box from '@mui/material/Box'

export default function AdminUsers() {
  return (
    <Box>
      <Typography variant="h4" sx={{ mb: 4, fontWeight: 'bold' }}>Usuarios</Typography>
      <Typography variant="body1">Gestión de usuarios en desarrollo.</Typography>
    </Box>
  )
}
